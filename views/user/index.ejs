 <div class="container d-grid mt-5">
       <div class="row">
        <% products.forEach((product)=> { %>
            <div class="card" style="width: 18rem;margin:15px">
                <img src="/uploads/<%=product.image%>" class="card-img-top" alt="...">
                <div class="card-body">
                  <h5 class="card-title"><%= product.name %></h5>
                  <p class="card-text"><%= product.description %></p>
                  <p class="card-price"><span id="<%= product._id %>"><%= product.price %></span>₹</p>
                  <button class="btn btn-primary" onclick="addToCart('<%= product._id %>')">Add to cart</button>
                </div>
              </div>
        <% }); %>
       </div>
 </div>
